import {
  NOTIFICATION_DISABLED,
  SOUND_ENABLED,
} from '../lib/constants';

export default {
  notification: {
    mode: NOTIFICATION_DISABLED,
    date: new Date(),
  },
  sound: {
    mode: SOUND_ENABLED,
    coachMode: SOUND_ENABLED,
    beepMode: SOUND_ENABLED,
  },
};
